// return a random number between 1 and 5
var words = Math.floor(Math.random() * 5 + 1);

// determine coordinate
var dims = getTheCordinates();
var originX = String(dims.width / 10 * 2);
var originY = String(dims.height * 1 / 4); 
var yRange = dims.height / 5;
var pathWidth = dims.width * .75;

// return a number of random points from 4 to 20
var numWordPoints = Math.floor(Math.random() * 16 + 4);
var wordPoints = '';
for (let point = 0; point < numWordPoints; point++) {
    let x = originX + point * (pathWidth / numWordPoints);
    let y = originY - Math.floor(Math.random() * yRange);
    wordPoints = x + ',' + y + '';
}


// concatenate words
for (let w = 0; w < words.length; w++) {
    const wordPath = words[w];
}